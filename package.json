{
  "name": "crm",
  "version": "1.0.0",
  "main": "index.js",
  "license": "UNLICENSED",
  "scripts": {
    "dev": "cross-env APP_ENV=dev shopify app dev --config dev",
    "test": "cross-env APP_ENV=test shopify app dev --config dev",
    "dev:debug": "cross-env APP_ENV=dev node index.js",
    "start": "shopify app dev",
    "info": "shopify app info",
    "generate": "shopify app generate",
    "deploy": "shopify app deploy"
  },
  "dependencies": {
    "@shopify/shopify-api": "^12.1.2",
    "cookie-parser": "^1.4.7",
    "dotenv": "^17.2.3",
    "ejs": "^3.1.10",
    "express": "^5.2.1"
  },
  "author": "linnzh",
  "private": true,
  "devDependencies": {
    "cross-env": "^10.1.0"
  }
}
